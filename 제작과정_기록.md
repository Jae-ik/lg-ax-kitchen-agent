# 제작 과정 기록

**팀 독립선언문 · Theme B (UX/UI 시나리오 설계자)** · 2026-09-22

고객 인사이트를 어떻게 찾았고, 어떤 도구를 썼고, **무엇이 틀렸고 어떻게 알아챘는지**를 적는다.
잘 된 것만 적으면 과정 기록이 아니라 홍보물이 된다.

---

## 1. 문제를 어떻게 좁혔나

### 출발점은 틀렸다

처음 잡은 주제는 **"손맛 학습 — 가족이 만든 요리를 재현하는 조리 시스템"** 이었다.
9/21에 그 방향으로 제안서를 제출했다.

9/22 문제 설명회에서 전제가 무너졌다.

> "이번 과제는 아이디어 제안이 아니라, **두 직무를 수행하는 Agent 개발**입니다."
> "미래 키친 콘셉트는 **Agent가 만들어낸 결과**입니다. 어떤 프로세스와 Skill로 반복 생성했는지가 핵심입니다."

우리가 만든 것은 **주방 기기를 제어하는 에이전트**였고, 요구된 것은 **UX 시나리오를 설계하는 에이전트**였다.
층이 하나 달랐다. 기기를 움직이는 일은 직무가 아니라 직무의 결과물이었다.

### 무엇을 버리고 무엇을 남겼나

| | 판단 |
|---|---|
| 버림 | "조리 재현 시스템"이라는 문서 골격 |
| 남김 | 근거(통계·특허 비교·포지셔닝), 기기 시뮬레이터, converge·aftercare 스킬 |
| 새로 만듦 | 설계 층 4개 스킬, 전제조건 플래너, 고객 상황 입력, 자료 수집 스킬 |

전면 재설계 대신 **기존 실행 층 위에 설계 층을 얹는** 2층 구조를 골랐다.
마감까지 5일이었고, 실행 층은 이미 두 기기에서 재사용이 실증돼 있었다.

---

## 2. 근거를 어떻게 모았나

### 원문까지 내려간다

인용한 수치는 전부 원문에서 확인했다. 2차 자료나 요약문을 근거로 쓰지 않았다.

| 자료 | 확인 방법 | 무엇이 걸렸나 |
|---|---|---|
| 조리태도 조사[8] | 학회지 PDF 직접 내려받아 Table 1 확인 | **내가 "조작된 수치"라고 잘못 판단했다** (아래 3절) |
| LG 특허[2] | Google Patents 청구항·명세 직접 읽음 | 제목을 잘못 적고 있었다 |
| Intel 특허[3] | 청구항에서 "재생 대상"이 제어 입력임을 확인 | 표에 "음식 상태 센서 없음"을 추가 |
| 조리 학습 출처[11] | 논문 Table에서 "어머니 60.1%" 확인 | — |

### 데이터는 합성이 아니라 공개 자료로

처음엔 조리 기록 3건을 직접 만들어 썼다. 합성 데이터만으로는 "데이터·근거에서 인사이트를 도출했는가"에 답할 수 없다.

**식품의약품안전처 조리식품의 레시피 DB(COOKRCP01)** 를 붙였다.
공개 `sample` 키가 있어 **키 발급 없이** 실데이터 100건을 받았다.

```
적재 100건 → 알레르기·기피(표고버섯·새우) 25건 제외
           → 나트륨 300mg 초과 27건 제외
           → 후보 48건
```

제외된 항목이 실제 레시피명으로 찍힌다: `새우 두부 계란찜 — 새우 포함`, `민들레 샐러드 — 874mg`.

---

## 3. 무엇이 틀렸고 어떻게 알아챘나

**개발 중 결함 11건을 잡았다.** 그중 8건은 자동 검증이, 3건은 원문 대조가 잡았다.

### 가장 큰 오판 — 내가 틀렸다

제안서의 조리태도 수치(3.94 / 3.03)를 **"근거 없이 만들어진 값"이라고 판단하고 통째로 바꿨다.**
학회지 홈페이지를 불완전하게 읽고 내린 결론이었다.

지적을 받고 원문 PDF를 직접 내려받으니 **Table 1에 두 값이 그대로 있었다.** 원래 수치가 맞았고 내가 틀렸다.
함정은 3.03이 우연히 두 가지를 동시에 가리킨다는 것이었다 — "요리는 쉽다" 문항 평균이면서, 저긍정군 복합 평균이기도 했다.

> **교훈**: 원문에 접근할 수 있는데 요약본으로 판단하지 않는다. 특히 "틀렸다"는 강한 주장을 할 때는 더욱.

### 검증 스킬이 잡은 것

`experience_verify`는 시나리오의 각 장면을 실행 로그와 대조한다. 장식이 아니라 실제로 설계를 세 번 되돌렸다.

| 무엇 | 어떻게 드러났나 | 조치 |
|---|---|---|
| 시나리오와 실행 불일치 | "못 먹는 재료를 미리 제외한다"고 써 놓고 실행은 조달까지 가서 확인 요구 → **5장면 중 3장면** | `menu` 단계로 필터를 앞당김 |
| 공개 레시피 경로 중단 | `record_get`이 `pub_639` 같은 ID를 몰라 **종료코드 1** | 공개 레시피용 변환 경로 추가 |
| 조리 실패 | `prep`이 소금·후춧가루를 재고 없음 처리 → **5장면 중 3장면** | 상비품을 재고 초기 상태에 포함 |

세 번째는 상비품 처리가 `menu`에만 있고 `prep`에는 없어서 생겼다.
**같은 가정을 두 곳에서 다르게 구현하면 생기는 결함**이고, 실행 대조가 없었으면 "왜인지 모르게 안 되는" 상태로 남았을 것이다.

### 조용히 지나갈 뻔한 것

| 무엇 | 왜 위험했나 |
|---|---|
| **죽은 조건문** `if b.get("at") is None` | `at`은 항상 있으므로 절대 참이 안 된다. 검증이 통과하는 게 아니라 **검증을 안 하고 있었다** |
| **측정 코드 설정 불일치** | converge 단독 측정은 7단계, 파이프라인은 8단계. 단독 측정에서 조달을 건너뛰어 두부가 계량에서 빠져 있었다. 안 잡았으면 **틀린 수치를 발표에 넣을 뻔** |
| **재료 파싱 54% 실패** | 원문에 표기가 두 종류였다. `연두부 75g` 와 `돼지고기(50g)` |
| **괄호 안 쉼표** | `닭고기(가슴살, 120g)` 를 쉼표로 자르면 수량을 잃는다 |
| **공개 API 응답 불안정** | `sample` 키가 같은 요청에 5건 또는 100건을 무작위로 준다. 한 응답을 믿지 않고 여러 번 받아 병합 |
| **문서 수치 오기 3건** | 특허 제목, 저자명(Hauser H→S), 학회지명(한국→대한지역사회영양학회지) |

---

## 4. 단일 실행을 근거로 쓰지 않기

수치를 제안서에 적기 전에 **흔들림을 먼저 쟀다.**

처음엔 "시드 3회"를 계획했다가, 3회로는 표준오차 자체의 상대오차가 50%라 분산 추정에 못 쓴다는 지적을 받았다.
시뮬레이터는 1회 390ms이므로 3회로 제한할 이유가 없었다. **500회 / 800회**로 올렸다.

| 구분 | 결과 |
|---|---|
| 조리기 가열 시간 | 500회 중앙 **7.8분** (7.6~8.1) |
| 건조기 건조 시간 | 500회 중앙 **10.8분** (10.3~11.2) |
| 계획 순서·메뉴·코스·개입 | 800회(200×4상황) **전부 불변** |
| 최종 질량비 | 0.7669~0.7800 — 목표 0.78을 **최대 0.013** 지나침 |

마지막 줄이 이 측정의 수확이다. "목표에 도달하면 즉시 종료한다"는 서술이 **1분 관측 주기 안에서만 참**이라는 것을 여기서 처음 알았다. 제안서 6절에 한계로 적었다.

---

## 5. 어떤 도구를 썼나

| 용도 | 도구 |
|---|---|
| 설계·구현·검증 | Claude Code (Opus 5) |
| 제안서 편집 | 한글 COM 자동화 (`pyhwpx`) — ASCII 경로 사본에서만 작업, 원본은 열지 않음 |
| 문서 검증 | PyMuPDF — PDF 렌더 후 삽입 문자열 전수 대조, 쪽별 채움률 측정 |
| 그림 | matplotlib (맑은 고딕, 흑백 출력 안전성 확인) |
| 자료 수집 | 식약처 OpenAPI, Google Patents, DOI 원문 |
| 알림 | Slack `#local` — 작업 단위마다 결과와 미확인 사항 기록 |

제출 경로(`run_design.py`)는 **표준 라이브러리만** 쓴다. `pip install` 대상 0개, 유료 API 0개.
소켓을 완전히 차단해도 끝까지 실행된다.

---

## 6. 지금 확실하지 않은 것

과정 기록이므로 미해결도 적는다.

- 흔들림은 **시뮬레이터 난수**에 대한 것이지 실제 조리의 변동이 아니다.
- 계획 불변(800회)은 난수가 계량·증발에만 들어가기 때문이기도 하다. **재고 구성이나 카탈로그를 흔들면 계획이 바뀔 수 있고, 그건 아직 안 재봤다.**
- 공개 레시피의 목표 질량비는 조리법별 기본값(가정)이다. 눌어붙음은 조리 중 실측으로 바꿨다.
- 조리기기의 **가열 제어**가 ThinQ 개인용 API에서 어디까지 열려 있는지 확인하지 못했다.
- LLM이 스킬을 고르는 경로(`agent.py`)는 API 키가 없어 실행 검증되지 않았다. 현재 제출 경로가 아니다.
- 개인 조리 기록 3건은 합성 데이터다.

## 9. 일부러 망가뜨려 본 뒤 (9/24)

정상 경로만 돌리면 "잘 된다" 밖에 말할 수 없다. `stress_test.py` 로 극단 입력
19건을 넣어 **예외는 0건**이었지만, 값을 읽으니 결함이 여섯 개 나왔다.

| 무엇이 나왔나 | 증거 | 무엇을 고쳤나 |
|---|---|---|
| 이미 목표에 도달했는데 한 번 더 가열 | `steps=1` | 시작 전에 확인하고 `steps=0` 으로 반환 |
| 용량을 1/100 로 줄이니 된장이 0g | `[('된장', 0)]` | 1g 하한 + `scale_warning` |
| **지나친 것을 도달로 셌다** | 목표 0.78 → 최종 **0.5474** 인데 `reached=True` | 허용 오차(0.10) 밖이면 `ok=False` |
| 1분 고정 관측이라 작은 냄비를 제어 못 함 | 102g 에서 목표를 크게 지나침 | 남은 시간이 한 주기보다 짧으면 **주기를 절반으로 줄인다**(최소 0.1분) |
| `menu` 가 재고 **이름만** 보고 수량을 안 봄 | 배추 20g 인데 200g 짜리를 고름 | 수량까지 대조해 모자란 것도 조달 대상에 넣음 |
| 1g 미만 양념이 계량에서 0g 으로 반올림 | 소금 0.2g → 0g | 저울 분해능 0.01g (1g 이상만 정수) |

가장 값이 큰 것은 세 번째와 네 번째다. **지나친 것을 성공으로 세고 있었다**는 것은
측정 자체가 틀렸다는 뜻이고, 그 위에 올린 수치는 전부 낙관 편향이 있었다.
고친 뒤 흔들림을 다시 재니 최종 질량비 범위가 **0.7295~0.7796 → 0.7669~0.7800** 으로
좁아졌다. 제안서에 "관측 주기만큼 지나친다" 고 쓴 한계가 실제로 완화됐다.

### 내가 한 실수 — 상위 지표로 하위 기전을 판정했다

`menu` 수량 확인을 넣은 뒤 회귀를 "단계 수 7 · 개입 2 · 장면 5/5" 로 확인하고
**통과**라고 적었다. 그런데 같은 실행에서 `가열 시간` 지표가 통째로 사라져 있었다 —
계량이 막혀 조리와 세척이 아예 실행되지 않았는데, **계획 단계 수는 7 그대로였기
때문에** 눈치채지 못했다. 계획 단계 수는 "계획이 7단계" 라는 뜻이지 "7단계가 다
돌았다" 는 뜻이 아니다.

그래서 `check_regression.py` 를 만들어 **실행 로그의 단계별 ok 와 최종 수치까지**
보게 했다. 계획에 있는데 실행되지 않은 스킬이 있으면 실패로 표시한다.
`scenarios.json` 의 항목 개수도 먼저 확인한다 — 실행이 중간에 죽으면 파일은
앞 실행 내용 그대로 남아 있어서, 옛 결과를 새 결과로 착각하게 된다.

### 냄비가 바뀌어도 자동인가

전에는 `run_share.py` 에 `CAPACITY = 1/3` 을 **손으로 적어** 두었다. 사람이 숫자를
넣는 한 "자동" 이 아니다. 기기가 자기 제원(인분·용량 상한)을 알게 하고,
`record_import` 가 두 기기의 제원에서 비율을 계산하도록 바꿨다. 지금은 대상 기기
이름만 주면 된다. 등록되지 않은 기기는 **추측하지 않고** 비율 1 로 두면서
"제원을 모른다" 고 말한다.

## 10. 남은 세 건을 마저 고치다 (9/24)

### 눌어붙음을 **재게** 했다 — 그리고 결론이 뒤집혔다

`soil_score` 는 조리를 끝내고도 **기록에 적힌 가정값**을 세척기에 넘기고 있었다.
"조리기가 아는 것을 세척기에 넘긴다" 가 제안의 논지인데, 조리기가 아는 것을
안 쓰고 있었으니 논지의 근거가 아니었다.

조리기가 매 관측마다 눌어붙음을 누적하게 했다 — 끓는 상태에서 수분이 줄수록,
화력이 셀수록 쌓인다. 실측값은 기존 가정값과 방향이 맞았다(조림이 가장 높음).

그런데 **결론이 바뀌었다.**

| | 가정값 | 실측 | 코스 | 기본 대비 |
|---|---|---|---|---|
| 된장찌개 | 0.62 | **0.552** | 표준 | **0 L** |
| 호박잎 삼계탕 | 0.55 | **0.134** | 에코 | **-3.5 L** |

삼계탕은 국물 요리인데 가정값 0.55 로 **강력을 돌리고 있었다.** 실측하니 에코로
충분했다. 반대로 된장찌개는 오염도를 알아도 표준이 맞아 **바꿀 것이 없다.**

즉 제안서에 쓴 "기대 물 18.2 L → 16.8 L, 1.4 L 절감" 은 **가정값을 쓴 결과**였고,
실측으로는 성립하지 않는다. 옳은 주장은 이것이다 —
**"오염도를 넘기면 항상 아낀다" 가 아니라 "기본 코스가 틀렸을 때 아낀다."**

### 임계로 고르던 것을 기대값으로 고치다

실측을 넣자 세척 코스가 **시드마다 갈렸다**(200회 중 표준 185 / 강력 15).
오염도 실측이 임계 0.55 바로 옆(0.5286±0.0133)에 앉아 있었기 때문이다.
점 하나를 임계와 비교하는 판정은 측정에 분산이 있으면 난수가 된다.

2σ 여유를 줘 봤지만 분포가 임계를 넓게 걸쳐 여전히 갈렸다(136/64).
그래서 임계 판정을 **버렸다.** 측정값과 분산으로 필요 강도의 **확률**을 구하고,
코스별 기대 물 사용량이 가장 작은 것을 고른다. 200회 중 **199회**가 같은 코스다.

임계라는 임의 상수가 결정에서 사라졌다. 남은 상수(0.30/0.55)는 이제
"어느 오염도까지 그 코스가 감당하는가" 라는 **코스의 제원**이지 판정 기준이 아니다.

### 소음 회피가 문장에만 있었다

`quiet_after` 는 시나리오 문장("22:30 전에 시작한다")에만 쓰이고 실행은 이 값을
보지 않았다. 게다가 그 문장 자체가 틀렸다 — 세척기 소음은 시작 시점이 아니라
**도는 내내** 난다. 22:25 에 시작한 145분 코스는 00:50 까지 돈다.

코스에 소음(dB)을 넣고, 조용 시각을 넘겨 도는데 시끄러우면 저소음으로 전환하게
했다(-8dB, 시간 +40%, 물 동일). p1 야근 가구만 발동한다. 시나리오 문장도
실제 동작과 맞췄다.

### 수렴에 실패하면 불을 끈다

`converge` 가 상한에 걸리면 실패만 알리고 **액추에이터를 그대로 두고** 돌아갔다.
호출자가 끄는 것을 잊으면 계속 가열된다 — 되돌릴 수 없는 과정에서 안전 문제다.
먼저 끄고, 왜 실패했는지 세 가지로 나눠 말한다: 정체 / 상한이 먼저 걸림 /
남은 시간이 지금까지보다 김. 그리고 멈춘 지점과 사유가 결과에 남는다 —
전에는 실패한 실행이 성공한 실행과 똑같이 "지표 없음" 으로 보였다.

## 11. "우리 요리잖아" — 요리로서 빠진 것을 채우다 (9/24)

질량비 하나만 쫓고 있었다. 그건 액체 졸이기지 요리가 아니다.
제안서를 다시 읽고 **적어 놓고 코드에 없는 것**부터 찾았다.

| 제안서 문장 | 코드 현실 |
|---|---|
| "재료 **투입 전후를 구분**한 뒤 같은 조리 단계의 목표와 현재 상태를 비교" | 조리 단계가 없었다 |
| 질량 신호 해석 조건: "**투입**·덜어냄·유출·교반" | 질량이 단조 감소한다고만 봤다 |
| "가열 상한, **이상 감지**, 사용자 개입 우선권을 먼저 구현" | 상한(화력 5)만 있었다 |

### 두부는 나중에 넣는다

된장찌개에서 두부를 처음부터 넣으면 부서진다. 조리법이 순서를 정하는 이유다.
투입은 두 가지를 바꾼다 — 질량이 **늘고**, 온도가 **떨어진다**(150g 두부가
들어가자 30도 하강). 분모(총 투입량)를 같이 늘리지 않으면 비율이 1 을 넘고,
기준을 다시 잡지 않으면 늘어난 질량을 '역행' 으로 읽어 ETA 가 무너진다.

`converge` 는 무엇을 언제 넣는지 모른다. 훅으로 물어보고, 일이 생기면
기준을 다시 잡는다. 가열 시간이 8.0 → 8.9분이 됐다 — 재가열이 필요하니까.

### 숙련자는 미리 끈다

불을 꺼도 냄비에 남은 열로 계속 끓는다. 그런데 예전 모델은 화력을 0 으로
하면 증발이 **즉시 멈췄다** — 미리 끌 이유가 없는 세계였다. 증발을 화력이
아니라 **온도**로 일어나게 고치고, 식는 속도를 데우는 속도보다 느리게 했다.
여열은 620g 에서 약 7g(질량비 0.011)이고, 기기가 스스로 예측한다(오차 0.37g).

작은 냄비는 빨리 식는다. 냉각 계수를 상수로 뒀더니 237g 짜리에서 여열을
620g 기준으로 과대평가해 **4.5분에 불을 껐다가 목표에 닿지 못했다.**
열용량은 질량에 비례하는데 표면적은 2/3 제곱으로만 늘므로 `m^(-1/3)` 로 고쳤다.

그리고 **여열이 모자라면 다시 켠다.** 지나친 것은 되돌릴 수 없지만 모자란
것은 되돌릴 수 있다. 빗나간 만큼 예측 신뢰도를 절반으로 낮춰 다시 잡는다.

### 여열 보정이 항상 좋은 것은 아니었다 — 재보고 알았다

"미리 끄면 더 정확하다" 를 그대로 믿고 넣었더니 **오차가 커졌다**
(0.0006 → 0.0057). 120시드로 관측 주기 하한과 교차해 쟀다.

| 주기 하한 | 여열 끔 | 여열 켬 |
|---|---|---|
| 0.1분 | **0.0030** | 0.0052 |
| 1.0분 | 0.0135 | **0.0049** |

자주 볼 수 있으면 목표 바로 앞까지 잘게 다가가면 되고, 그때 여열을 미리
빼는 것은 예측 오차를 들여오는 일이다. 드물게 보면 반대다. 그래서
`converge` 가 **한 걸음의 크기와 여열 크기를 비교해 스스로 정한다.**
각 조건에서 더 나은 쪽과 같은 값이 나온다.

### 4인 가구가 1인분을 끓이고 있었다

`household_size` 는 '세척까지 할지' 를 정하는 데만 쓰였다. 그래서 4인 가구가
공개 레시피 1인분(237g)을 그대로 조리했다 — 한 사람 몫도 안 된다.
**몇 인분을 만들지는 조리의 첫 결정**인데 그것이 빠져 있었다.

기록에 인분 수를 적고, 가구원 수에 맞춰 늘리거나 줄인다. 기기 용량이
상한이다(85% 까지). 그 결과 조리량·가열 시간·세척 코스가 상황마다 갈렸다 —
1인 가구 310g 7.0분 에코, 4인 가구 960g 8.3분 표준.

### 넘치는 것은 상한으로 막을 수 없다

화력 상한 5 는 **기기 사양**이지 상황 판단이 아니다. 국물이 가득 찬 냄비에서
5까지 올리면 넘친다. 채움 비율 × 끓음 × 화력으로 위험을 재고 화력을 묶는다.

처음엔 한 번 묶으면 **영원히 묶인 채**여서, 가득 찬 냄비 시험에서 화력 2 로
40분을 돌다가 목표에 닿지 못했다. 위험이 지나가면 되돌리게 고쳤다(17분).

파이프라인에서는 이 감지가 한 번도 뜨지 않는다 — 조리량을 정할 때 이미
용량의 85% 로 제한하기 때문이다. 이중 안전장치이고, 그래서 가득 채운
스트레스 시험으로만 검증된다.

### 확인하고 접은 것

- **염도**: 저염 가구에게 졸이기를 시키면서 농도를 안 본다고 생각했는데,
  졸여도 **총 나트륨과 인분 수는 그대로**라 1인분 나트륨은 변하지 않는다.
  변하는 것은 짠맛 농도뿐이고, 제안서가 이미 "질량 감소만으로 염도를
  확정하지 않는다" 고 한계로 적었다. 건드리지 않았다.
- **나트륨 기준**: 식약처 자료의 1회 제공량(`INFO_WGT`)이 **95/100 결측**이라
  나트륨이 어느 양 기준인지 자료로 대조할 수 없다. 값이 있는 5건은
  찌개 200g/385mg 로 1인분을 시사하지만 확인된 것은 아니다. 한계로 남긴다.

## 12. "기록도 여열을 계산했을 텐데" — 시점이 정의되어 있지 않았다 (9/24)

여열을 구현하고 "관측을 자주 할 수 있으면 여열 보정이 해롭다" 는 측정까지
내놓았는데, 지적을 받고 보니 **기록이 어느 시점의 값인지** 정의하지 않은 채
여열을 논하고 있었다.

재 보니 **불 끄는 순간 0.7789, 먹기 직전 0.7686 — 0.0103 차이**다.
그런데 저장하는 값은 앞의 것이었다. 사람이 "이 상태가 좋다" 고 하는 것은
먹은 상태인데, 기록에는 불 끄는 순간이 남았다.

이 불일치는 **회차마다 누적된다.** 저장값을 다음 목표로 삼으면 그 지점에서
또 여열이 붙기 때문이다. 5회 반복하니 실제로 먹는 상태가
0.7654 → 0.7599 로 흘러갔다.

### 고친 것과 그 결과로 뒤집힌 것

기록을 **먹기 직전**(여열이 끝난 뒤)에 재도록 고쳤다. 그러자 앞선 측정의
전제가 무너졌다 — "여열 보정이 해로울 수 있다" 는 결론은 **목표가 불 끄는
순간의 값이라는 가정** 위에 있었다. 목표가 먹기 직전 값이면 여열은 선택이
아니라 필수다. 자동 판단을 넣었던 것을 도로 뺐다.

| 관측 주기 하한 | 여열 끔 | 여열 켬 |
|---|---|---|
| 0.1분 | 0.0161 | **0.0034** |
| 0.5분 | 0.0226 | **0.0139** |
| 1.0분 | 0.0425 | 0.0425 (발동 못 함) |

### 다시 재는 중에 나온 물리 버그

여열 예측(8.28g)과 실제(3.5g 정도)가 2배 넘게 어긋났다. 원인은
**온도 갱신이 스텝 크기를 무시**하는 것이었다.

```python
self.temp_c += (target - self.temp_c) * k        # k 는 1분당 비율인데
```

`tick(0.5)` 도 `tick(1.0)` 과 **같은 양만큼** 식었다. 같은 5분을 1분 단위로
가면 88.9도, 0.25분 단위로 가면 66.5도가 나왔다 — **관측을 자주 할수록 빨리
식는 세계**였다. 적응 주기를 도입한 순간부터 있던 버그이고, 그 위에서 잰
수치는 전부 주기에 오염돼 있었다.

지수 감쇠의 올바른 이산화(`1 - (1-k)**minutes`)로 고치고, 증발량은 스텝
동안의 **평균 온도**로 구하게 했다(끝 온도만 쓰면 큰 스텝에서 20% 과소).
이제 1.0 / 0.5 / 0.25 / 0.1분 어느 단위로 가도 8.90g 으로 같다.

### 목표는 흘러가면 안 된다

여열을 제대로 보정해도 평균 0.0034 는 지나친다. 그 값을 다음 목표로 삼으면
편향이 회차마다 쌓여 8회 뒤 0.78 이 0.7509 가 됐다.

**목표는 가정값일 때만 실측으로 갈아 끼운다.** 이미 실측인 목표는 유지한다.
"엄마 된장찌개" 의 목표는 한 번 정해지면 매번 바뀌지 않는다 — 목표를 바꾸는
것은 사용자가 "더 졸여줘" 라고 할 때지 기계가 조금 빗나갔을 때가 아니다.
8회 반복 결과가 0.7692~0.7792 로, 목표 0.78 주위에서 흔들릴 뿐 흘러가지 않는다.

## 13. 안 본 요리 디테일 다섯 가지 (9/24)

### 물을 먹는 재료 — 질량비가 거짓말을 한다

찹쌀 400g 은 국물을 800g 쯤 먹는다. 먹어도 **총 질량은 그대로**라 질량비만
보면 아무 일도 없다. 그런데 졸일 물은 사라지고 있다.

물을 안 채우고 돌려 보니 **질량비 0.9336 에서 멈추고 눌어붙음이 1.0** 이 됐다.
목표 0.85 는 영원히 오지 않는다. 자유 수분(= 총 질량 − 고형분 − 흡수분)을
따로 추적하게 하고, 바닥나면 제어기가 멈추도록 했다.

물을 얼마나 부어야 하는지는 계산으로 나온다. 총량 M 중 고형 S 와 흡수 A 는
졸일 수 없으므로, 목표 비율 r 까지 가려면 `M ≥ (S+A)/r` 이어야 한다.
흡수량(800g)만 채웠다가 또 바닥나서 알았다 — 삼계탕에는 **1088g** 이 필요했다.

### 뚜껑 — 제어 변수가 하나뿐이었다

화력만 조절하고 있었다. 실제로는 끓을 때까지 덮고, 끓으면 열어야 졸는다.
처음엔 열손실 계수만 키웠더니 6분 뒤 78.5도 vs 79.0도로 **차이가 거의 없었다.**
뚜껑은 오르는 속도가 아니라 **도달 온도**를 바꾼다. 고치니 78.5도 vs 97.0도다.

그리고 온도 한 점(96도)을 기준으로 여닫게 했더니 그 근처에서 계속 뒤집혀
제어가 무너졌다(0.78 목표에 0.61). 한 번 열면 끝까지 열어 두는 것으로 바꿨다.

뚜껑을 여는 순간 증발이 6배로 뛴다. 직전 속도로 잰 ETA 가 쓸모없어져 한 걸음에
목표를 크게 지나쳤다(0.65). 조건이 급변하는 일 뒤에는 **주기를 줄여 다시
파악**하도록 훅에 표시를 붙였다.

### 거품 걷기 — 제안서에 적어 놓고 없던 것

제안서 표3 은 질량 신호의 해석 조건으로 "투입·**덜어냄**·유출·교반" 을 적었다.
덜어냄이 없었다. 고기를 삶으면 거품이 뜨고, 걷어내면 질량이 준다 —
**증발이 아닌데 제어기는 졸은 것으로 읽는다.** 분모에서도 빼 준다.
1000g 에서 45g 을 걷어도 비율은 1.0 그대로다.

### 젓기 — 눌어붙음이 시간만의 함수였다

눌어붙음을 시간·화력·수분으로만 계산했다. 안 저으면 바닥에 가라앉은 것이
탄다. 저은 뒤 3분 동안은 눌어붙음이 45% 로 준다(0.092 → 0.0506).
교반은 제안서 표1 에서 **사람이 하는 일**로 적었으므로, 에이전트는
"지금 한 번 저어 주세요" 를 안내한다 — 개입이 아니라 안내다.

### 물로 되돌리기 — 되돌릴 수 없다는 가정이 틀렸다

졸이는 것은 되돌릴 수 없다고 보고 "지나치면 끝" 으로 처리해 왔다.
요리에는 되돌릴 수단이 하나 있다. **물을 더 붓는 것이다.** 다만 공짜가
아니라 국물이 묽어진다. 얼마나 묽어졌는지를 함께 남기고, 6% 를 넘으면
되돌리지 않고 지나친 채로 알린다.

되돌리기는 **여열이 끝난 뒤** 해야 한다. 불 끈 시점에 맞춰 부었더니 남은
열로 다시 졸아 오차가 그대로였다(0.0183). 여열 뒤로 옮기니 0.0000 이다.
다만 이것은 제어가 정확해진 것이 아니라 **물을 부어 맞춘 것**이고,
국물이 1~3% 묽어졌다. 그 사실을 결과에 함께 적는다.

### 곁가지로 나온 것

- **상한은 관측 횟수가 아니라 시간이어야 한다.** 주기를 줄이면 18.75분짜리
  조리도 60회를 채워 멈췄다. 사용자의 시간 예산을 상한으로 쓴다.
- **훅이 발동할 때마다 주기를 1분으로 되돌리면 안 된다.** 뚜껑·안내처럼
  기준이 그대로인 일까지 되돌리면, 목표 앞에서 잘게 다가가던 것이 매번 풀린다.
- **회귀 검사가 옛 파일을 읽고 통과라고 했다.** 생성 명령이 예외로 죽었는데
  `scenarios.json` 이 그대로 남아 있었다. 내가 CLAUDE.md 에 적어 둔 함정에
  그대로 걸렸다. 파일 생성 시각 확인을 검사에 넣었다.

### 남은 것 — 세척 코스가 갈린다

1인 가구(310g)의 눌어붙음이 **0.311±0.0144** 로 코스 경계(0.30) 바로 위에
앉았다. 60회 중 표준 43 / 에코 17 로 갈린다. 기대 물 사용량으로 고르게
해 두었는데도 갈리는 것은 **실제로 경계 요리이기 때문**이고, 두 선택의
기대값 차이도 2L 남짓이다. 억지로 고정하지 않고 갈린다는 사실을 적는다.

## 14. 누가 요리하는가, 그리고 열 모델을 갈아엎다 (9/24)

### 로봇이 아닌데 로봇처럼 굴고 있었다

"조리는 로봇이 하는 가정인가 사람이 하는 가정인가" 라는 질문을 받고 코드를
다시 보니 **일관성이 없었다.**

| 동작 | 제안서 표1 | 코드 |
|---|---|---|
| 화력 조절 | 시스템 | 시스템 ✓ |
| 재료 투입 | **사람** | 코드가 자동으로 넣음 |
| 뚜껑 | (명시 없음) | 코드가 자동 |
| 거품 걷기 | (명시 없음) | 코드가 자동 |
| 교반 | **사람** | 알림 후 코드가 실행 |

제안서는 "조리 전체를 자동화하는 것이 아니라 **판단 부담을 옮기는 제안**"
이라고 못 박았는데, 코드는 손까지 대신 쓰면서 "개입 0회" 를 자랑했다.

가전이 실제로 할 수 있는 것은 **화력 조절과 판단·기록**뿐이다. 나머지는
사람의 손이 필요하고, 에이전트의 값은 대신 하는 것이 아니라 **언제 해야
하는지 알려주는 것**이다. 그래서 값은 "개입 0회" 가 아니다.

| 상황 | 손이 가는 일 | 냄비 앞에 있어야 하는 시간 |
|---|---|---|
| 야근 1인 | 3회 | **3분** / 조리 9.0분 |
| 알레르기 4인 | 6회 | **6분** / 조리 24.45분 |

에이전트가 없으면 언제 해야 할지 모르므로 조리 내내 지켜봐야 한다.
값은 **"지켜보는 시간이 24.45분에서 6분으로"** 다. 시나리오 문장의
"불 앞을 지키지 않는다" 도 "부를 때만 손을 댄다" 로 고쳤다.

### 열 모델을 1차 지연에서 열량 수지로

앞선 라운드에서 "1차 지연이라 출발 온도 차이가 사라진다" 를 한계로 적었는데,
**정확해야 한다는 지적을 받고 갈아엎었다.**

예전 식 `T += (목표온도 - T) * k` 의 문제는 두 가지다.
- 양이 달라도 데우는 시간이 같다 (310g 과 2000g 이 둘 다 3.75분)
- 출발 온도 차이가 지수적으로 사라진다 (냉장 재료를 넣으나 마나)

실제 화구는 **일정한 열량**을 넣는다.

```
dT/dt = (투입 − 방열) / (유효질량 × 비열)
끓는점에 닿으면 남는 열은 온도가 아니라 증발로 간다 (잠열 2260 J/g)
```

상수는 두 가지 실제 값에서 역산했다 — 620g 을 불 끄면 100→90도가 약 3분
(방열 2.28 W/K), 화력 3 으로 20→100도가 약 5.5분(화력당 298 W).

| | 고친 뒤 |
|---|---|
| 310g / 620g / 2000g 끓기까지 | **3.5 / 5.5 / 15.0분** |
| 20도 출발 vs 6도 출발 | **5.5분 vs 6.5분** (차이 유지) |
| 화력 3 끓는 중 증발 | 20.9 g/분 (옛 모델 21.0) |
| 스텝 크기 1.0/0.5/0.25/0.1분 | 결과 일치 |

증발량이 옛 모델과 거의 같게 나온 것은 우연이 아니다 — 옛 계수도 같은
실제 값에 맞춰 잡았기 때문이다. 달라진 것은 **양과 출발 온도에 대한 반응**이다.

### 예측이 실제를 오염시키고 있었다

여열 예측을 "가상 복사본에 같은 tick 을 돌린다" 로 바꿨더니 예측이 **전역
난수를 소비**했다. 예측을 몇 번 했느냐에 따라 실제 조리 결과가 달라진다
(566.0g vs 565.8g). 복사본에는 회차 편차를 넣지 않도록 플래그를 뒀다.

예측을 별도 식으로 적어 두면 본체를 고칠 때 어긋난다 — 실제로 한 번 2배
틀렸다. 그래서 같은 코드를 쓰되, 부작용만 차단하는 쪽을 골랐다.

## 15. 열 모델을 갈아엎고 나서 남은 것들 (9/24)

"오류 다 고쳤나" 는 물음에 답하려고 스트레스 39건의 **값을 처음부터 다시
읽었다.** 예외는 0건인데 값에서 다섯 개가 나왔다. 전부 열 모델 교체가
만든 것이거나, 교체로 드러난 것이다.

| 무엇 | 값 | 원인 |
|---|---|---|
| 여열이 **두 번** 적용 | 여열O 0.0115 > 여열X 0.0084 | `converge` 가 끈 뒤에도 붙들고, 호출자가 또 돌렸다 |
| 여열 구간이 **정의되지 않음** | 15분 두면 11.3g | 끓음이 멎어도 표면 증발이 계속돼 끝나는 지점이 없다 |
| 5g 을 태움 | 목표 0.78 → **0.0523** | 제어 불가를 경고만 하고 진행했다 |
| 유지 화력이 낡음 | 목표 0.95 → 0.7777 | 옛 모델 기준 3 이었는데, 새 모델은 화력 1 도 끓는다 |
| 시험 문구가 옛 결론 | "출발 온도 차이가 지워진다" | 고쳐 놓고 시험 설명을 안 고쳤다 |

### 여열은 두 번 적용되고 있었다

`converge` 가 여열을 예측해 미리 끄고, **그 뒤 여열 구간까지 관측**했다.
그리고 도메인이 다시 `rest_until_still` 을 돌렸다. 같은 여열이 두 번 붙으니
여열을 쓰는 쪽이 오히려 나빴다.

역할을 나눴다. `converge` 의 일은 **'언제 끌지' 까지**다. 끄면 그 시점 값과
예상 도착값을 돌려주고 끝난다. 여열은 호출자가 한 번만 돌린다.
고치니 0.0084 → **0.0031** (2.7배).

### 여열 구간을 사람의 행동으로 정의했다

1차 지연 모델일 때는 "끓음이 멎으면" 이 자연스러운 끝이었다. 열량 수지로
바꾸자 표면 증발이 계속돼 끝이 사라졌다. 여열의 크기는 물리가 정하지만
**얼마나 두는가는 사람이 정한다.** "불 끄고 상에 올리기까지 3분" 으로
명시했다. 예측 오차가 0.01g 이 됐다.

### 경고는 막지 못한다

제어 가능 최소량 미달을 **경고만** 하고 진행했더니, 5g 짜리가 목표 0.78 을
노리다 0.0523 까지 졸아 버렸다. 한 걸음보다 졸일 양이 적으면 맞출 방법이
없다 — **시작하지 않는 것이 맞다.** 거부하고 이유를 돌려준다.

### 모델을 바꾸면 그 위의 상수도 낡는다

익힘을 기다리는 유지 화력을 3 으로 잡은 것은 **옛 모델에서 화력 1 이 53도로
식었기 때문**이었다. 새 모델에서는 화력 1 도 끓는다(평형 151도). 3 으로 두면
유지하는 동안 분당 3.4g 이 날아가 목표 0.95 짜리가 0.7777 까지 갔다.
화력 1 + 뚜껑으로 1.0g/분까지 줄이니 0.909 로 들어왔다.

"졸임이 끝났는데 안 익었으면 뚜껑을 덮는다" 도 이때 넣었다 — 실제 조리의
"뚜껑 덮고 뭉근히" 가 그것이고, 덮지 않으면 익히는 동안 계속 졸는다.

## 16. 낡은 것을 전부 찾아 고치다 (9/24)

"낡았으면 나머지도 다 업데이트하면 되는 거 아니야" — 맞다. 열 모델에
의존하는 상수를 **grep 으로 전수 조사**해 하나씩 다시 쟀다.

| 상수 | 근거였던 것 | 새 모델 실측 | 조치 |
|---|---|---|---|
| `min_ctrl` 2.1g | 화력3 한 걸음 | 화력3 **2.00g** / 화력5 **3.80g** | 최대 화력 기준으로 계산 |
| `ready_at` 92도 | 옛 모델 끓음 시작 | 94.6도 1.0g / **100도 11.6g** | **99.5도** |
| `ETA_SAFETY` 1.4 | 가속 폭 | 끓는 중 최대 **1.59** | **1.7** |
| soil `min(1.0,…)` | — | 24분 조리가 **1.0 포화**, σ=0 | `1-exp(-누적)` |
| 넘침 임계 0.30/0.45 | — | 채움 **50% 가 이미 0.462** | **0.60/0.78** |
| `ABSORB` 시작 60도 | 익힘 기준을 그대로 씀 | 2kg 에서 **6분까지 흡수 0** | **40도** |
| `hold_power` 3 | 옛 모델에서 1은 53도로 식음 | 새 모델은 **화력 1도 끓는다** | **1** |
| `demo_offline` 92 | 옛 끓음 판정 | 같은 이유 | **99.5** |
| `STAGE_COSTS` 조리 12분 | — | 실제 5.5~25.25분 | 유지 — **설계 시점 추정값**임을 명시 |

### 죽은 코드가 생겨 있었다

`converge` 를 "언제 끌지까지" 로 자르면서 **여열 재가열 루프가 도달 불가**가
됐다. `relights`·`res_trust`·`coasting_at`·`MAX_RELIGHT` 가 초기값에서
변하지 않는 채 남아 있었고, 반환 dict 에 `guard_notes` 키가 **중복**돼
있었다(뒤의 것이 앞의 것을 덮어쓴다).

그리고 그 과정에서 **덜 졸았을 때 복구 경로가 사라진 것**을 발견했다.
물을 붓는 것은 지나쳤을 때 쓰는 수단이고, 모자랄 때는 더 졸이면 된다.
도메인에 재가열 루프를 넣었다.

### 최소 관측 주기는 이제 의미가 없다

`min_interval` 을 0.05 / 0.1 / 0.25 로 바꿔 재니 **결과가 같았다**
(먹기 직전 0.7781, 관측 9회). 여열을 예측해 미리 끄면 목표 앞에서 잘게
다가갈 필요가 없기 때문이다. 잘게 보는 것으로 정확도를 사던 구조가,
**미리 끄는 것으로 정확도를 얻는 구조**로 바뀌었다.

### 상수마다 근거를 적어 뒀다

같은 일이 또 생기지 않게 `kitchen.py` 상단에 표를 넣었다 — 어떤 상수가
측정에서 나왔고, 어떤 것이 가정이며, **모델을 바꾸면 함께 낡는 것**이
어디에 있는지. 근거를 못 적는 숫자는 다음 사람이 고칠 수 없다.

최종 상태: 스크립트 6종 종료 코드 0 · 회귀 4상황 오차 0.0000~0.0002 ·
스트레스 39건 · 흔들림 260회 계획·메뉴·코스·개입·장면 전부 불변.

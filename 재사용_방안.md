# 재사용 방안

**팀 독립선언문 · Theme B**
작성 2026-09-22

재사용 주장을 **세 등급으로 구분**한다. 실제로 돌려본 것과 구조상 가능한 것과 설계만 한 것을 섞지 않는다.

| 등급 | 뜻 |
|---|---|
| **A. 실증** | 같은 코드로 두 대상에 돌려 결과를 측정했다 |
| **B. 구조** | 교체 지점이 코드에 있고 도메인 결합이 없다. 아직 돌려보지 않았다 |
| **C. 설계** | 설계만 했다. 근거로 쓰지 않는다 |

---

## A. 실증 — 같은 스킬, 다른 가전

`run_demo.py` 실행 결과. **스킬 코드는 한 글자도 다르지 않다.**

| Skill | 대상 1 | 대상 2 | 바꾼 것 |
|---|---|---|---|
| `converge` | 조리기 — 목표 질량비 0.78, **7.65분**에 도달 | 건조기 — 목표 함수율 0.08, **11.3분**에 도달 (최종 0.0795) | 관측 함수·액추에이터 함수만 주입 교체 |
| `aftercare` | 식기세척기 — 실측 오염도 0.178 → `에코` | 세탁기 — 오염도 0.36 → `표준` | 코스 프로파일 표만 교체 |

`converge`가 가진 것은 **"관측 → 목표 대조 → 액추에이터 조정 → 재관측"** 구조뿐이다.
무엇을 관측하고 무엇을 조작할지는 호출자가 함수로 넣는다. 그래서 기기가 바뀌어도 스킬이 바뀌지 않는다.

```python
# 조리기
converge(observe=lambda: COOKER.state(), actuate=COOKER.set_power,
         step=COOKER.tick, metric="mass_ratio", target=0.78)
# 건조기 — 같은 스킬, 주입만 교체
converge(observe=lambda: DRYER.state(), actuate=DRYER.set_power,
         step=DRYER.tick, metric="moisture", target=0.08)
```

**확장 가능한 대상**: 제습기(습도), 에어컨(체감온도), 정수기(출수량), 오븐(중심 온도)
— 모두 "목표값으로 수렴시킨다"는 같은 구조다.

---

## A-2. 실증 — 자료 수집 스킬

`recipe_source`는 "자료를 불러와 제약으로 거른다"는 구조만 가진다.
무엇을 불러올지(`load`)와 원문을 어떻게 검사할지(`match`)를 주입받는다.

| 주입 | 지금 | 바꾸면 |
|---|---|---|
| `load` | `data/recipes.json` 캐시 | 제품 리뷰 API, 특허 검색, 논문 초록 |
| `match` | 재료 원문의 기피어 검사 | 리뷰의 불만 키워드, 특허 청구항의 구성요소 |

실제 실행 결과 — 식약처 레시피 DB 100건에서
**알레르기·기피로 25건, 나트륨 상한으로 27건을 걸러 48건**을 남겼다.
같은 스킬이 Theme A 의 "리뷰 수집 → 니즈 군집화" 에서 첫 단계가 된다.

---

## B. 구조 — 같은 설계 층, 다른 도메인

설계 스킬 4종은 주방을 모른다. 도메인 지식은 전부 **`kitchen_domain.py` 한 파일**에 있다.

```
skills/design_skills.py   ← 주방이라는 단어가 없다
kitchen_domain.py         ← 전제조건·효과·인자 바인딩. 여기만 바꾸면 도메인이 바뀐다
```

| 바꾸는 것 | 주방 | 세탁실 (예) |
|---|---|---|
| 단계 비용 | 보관 확인·메뉴 결정·조달·준비·조리·세척 | 분류·세탁·건조·의류관리 |
| 목표 사실 | `cooked`, `cleaned` | `washed`, `dried`, `styled` |
| 작업 바인딩 | inventory→menu→procure→prep→converge→aftercare | sort→wash→converge(건조)→aftercare(세탁조) |

`situation_read`가 읽는 것은 "시간 예산·다음 일정·제약"이라는 **생활의 구조**이지 요리가 아니다.
그래서 아침 준비 루틴, 귀가 루틴, 수면 루틴에 같은 스킬이 그대로 붙는다.

> 이 등급은 **아직 실행하지 않았다.** 세탁실 도메인 파일을 쓰면 확인할 수 있고, 그것이 다음 작업이다.

---

## C. 설계 — 같은 Agent 구조, 다른 직무 (Theme A)

Theme A(AI 상품기획 담당자)는 단계가 다를 뿐 구조가 같다.

| | Theme B (구현함) | Theme A (구조만) |
|---|---|---|
| 1 | `situation_read` 고객 상황 이해 | `collect` 리뷰·논문·특허 수집 |
| 2 | `scenario_draft` 시나리오 작성 | `cluster` 반복되는 니즈 군집화 |
| 3 | `flow_design` 서비스 흐름 설계 | `match` 타 산업 기술과 연결 |
| 4 | `experience_verify` 경험 검증 | `concept_verify` 근거 대조 |

`planner.py`와 `Registry`는 도메인·직무와 무관하다. Task 목록만 갈아 끼우면 된다.
**단, 이 등급은 근거로 쓰지 않는다.** 돌려보지 않았다.

---

## 실제 제품으로 옮기는 경로

시뮬레이터(`kitchen.py`, `dryer.py`)를 LG ThinQ Connect API 호출로 바꾸면 된다. 스킬은 그대로다.

| 항목 | 확인된 내용 |
|---|---|
| 규격 | ThinQ Connect — 클라우드 기반 RESTful Web API |
| 대상 기기 | 오븐·식기세척기·냉장고·세탁기·건조기 등 24종 |
| 인증 | Personal Access Token (PAT) + Client ID + 국가코드 |
| 개발자 사이트 | `thinq.developer.lge.com` / `smartsolution.developer.lge.com` |
| Python SDK | `pip install thinqconnect` (배포자 연락처 `thinq-connect@lge.com`, 1.0.14) |
| 구분 | 개인용 ThinQ API / 기업 파트너용 ThinQ Business API |

교체 지점은 `kitchen_domain.py`의 바인딩 함수 6개뿐이다.

```python
# 지금 (시뮬레이터)
"observe": lambda: K.COOKER.state()
# 실제 기기 — 같은 스킬, 주입만 교체
"observe": lambda: thinq.get_device_status(oven_id)
```

> 조리기기의 **가열 제어**가 개인용 API에서 어디까지 열려 있는지는 아직 확인하지 못했다.
> 상태 조회만 가능하다면 `converge`는 조절 안내까지만 수행하고, 실제 가열은 사용자가 승인한다.
> 이 구분은 발표에서 명시한다.

---

## 업무 가치

| 지금 | 이 구조 이후 |
|---|---|
| 제품마다 제어 로직을 따로 만든다 | `converge` 하나를 공유한다 (조리기·건조기 실증) |
| 상황이 바뀌면 시나리오를 사람이 다시 그린다 | 상황을 입력하면 계획과 시나리오가 다시 계산된다 |
| 시나리오가 실제로 되는지 별도로 확인한다 | `experience_verify`가 장면마다 실행 로그와 대조한다 |

마지막 항목은 개발 중 실제로 작동했다. 알레르기 가구 시나리오에서 "못 먹는 재료를 미리 제외한다"고 써 놓고 실행에서는 조달 단계까지 가서 사용자 확인을 요구하는 불일치가 있었고, **검증 스킬이 `5장면 중 3장면 달성`으로 이를 잡아냈다.** 그래서 `menu` 단계에 필터를 앞당겨 넣었다.

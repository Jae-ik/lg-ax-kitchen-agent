# 왜 이렇게 만들었나 — Agent와 스킬 11개

발표에서 질문받을 때 답할 수 있도록 **"왜 만들었나 / 무슨 판단을 하나 / 없으면 무슨 일이 생기나"** 순으로 적는다.
모든 수치는 코드에서 그대로 가져왔다.

---

## 0. 한 문장

> **고객 상황을 입력받아, 가전들이 나눠 맡을 일을 스스로 계획하고, 실행한 뒤, 계획대로 됐는지 자기가 검사하는 에이전트.**

"된장찌개 해줘"를 받는 게 아니다. **"귀가 21:40, 쓸 수 있는 시간 25분, 다음 날 아침 바쁨"** 을 받는다.
무엇을 할지는 에이전트가 정한다.

---

## 1. 왜 두 층으로 나눴나

처음에는 한 층이었다. 기기를 제어하는 스킬만 있었다.
그런데 해커톤이 요구한 직무는 **"UX/UI 시나리오 설계자"** — 가전을 돌리는 사람이 아니라 **경험을 설계하는 사람**이다.

```
설계 층 (직무)   상황을 읽고 → 시나리오를 쓰고 → 흐름을 설계하고 → 검증한다
      ↓  실행 함수를 인자로 받는다 (import 하지 않는다)
실행 층 (기기)   자료수집 · 보관 · 메뉴 · 조달 · 준비 · 조리 · 세척
```

**설계 층 코드에 "조리기"나 "냉장고"라는 말이 한 번도 안 나온다.**
무엇을 실행할지는 밖에서 함수로 넣어준다. 그래서 같은 설계 층을 세탁실·욕실에 그대로 얹을 수 있다.

> 질문받으면: "설계와 실행을 섞으면 주방 전용 코드가 됩니다. 분리했더니 설계 층이 도메인을 모르게 됐고, 그게 재사용의 조건이 됐습니다."

---

## 2. 설계 층 4개 — 직무를 네 단계로 쪼갠 것

해커톤이 제시한 Theme B 단계(고객 상황 이해 → 시나리오 작성 → 서비스 흐름 설계 → 경험 검증)를 그대로 스킬로 만들었다.

### ① `situation_read` — 상황에서 제약을 뽑는다

| | |
|---|---|
| **왜** | 고객이 "불편하다"고 말한 것만으로는 무엇을 없앨지 정할 수 없다. 시간·가구원·제약을 **숫자로** 바꿔야 판단이 가능하다 |
| **판단** | 필요 시간 합계와 가용 시간을 비교한다. `가용 25분 < 필요 43분` → **조달을 빼기로 결정**<br>다음 날 아침이 바쁘거나 2인 이상 → **세척까지 완료를 목표에 포함**<br>알레르기가 있으면 → 조달 안전 필터 강화 |
| **없으면** | 모든 가구에 같은 계획이 나온다. 25분뿐인 사람에게도 장보기를 시킨다 |

**이 스킬이 1인 가구의 6단계를 만든 출발점이다.**

### ② `scenario_draft` — 수고가 사라진 하루를 먼저 그린다

| | |
|---|---|
| **왜** | 기기부터 정하면 "기술로 할 수 있는 것"에 갇힌다. **고객이 겪을 장면을 먼저 쓰고** 거기에 맞춰 기기를 배치해야 한다 |
| **판단** | 장면마다 `사용자가 하는 일 / 가전이 하는 일 / 사라진 수고`를 적는다. **그리고 그 장면을 나중에 누가 증명할지(`verified_by`)를 같이 적어둔다** |
| **없으면** | 검증할 기준이 없다. "잘 됐다"를 무엇과 비교할지 모른다 |

`verified_by`가 이 스킬의 핵심이다. 그림이 아니라 **나중에 대조할 수 있는 약속**을 쓴다.

### ③ `flow_design` — 목표를 정하고 순서를 **계산**한다

| | |
|---|---|
| **왜** | 시나리오는 "이랬으면 좋겠다"이고, 그걸 실현할 가전 작업 순서는 따로 정해야 한다 |
| **판단** | 목표 사실을 정한다 — 항상 `조리`, 세척까지면 `세척` 추가.<br>조달을 빼기로 했으면 **`재고 확보`를 "이미 참"으로 놓는다**. 그러면 플래너가 조달을 끌어올 이유가 없어진다 |
| **없으면** | 순서를 사람이 코드에 적어야 한다. 상황이 바뀌면 코드를 고쳐야 한다 |

### ④ `experience_verify` — 그림과 실행을 대조한다

| | |
|---|---|
| **왜** | 시나리오는 **그림**이고 실행 로그는 **사실**이다. 둘이 어긋나면 설계가 틀린 것인데, 대조하지 않으면 그걸 모른다 |
| **판단** | 장면마다 `verified_by`에 적힌 스킬이 실제로 실행됐고 성공했는지 본다. 하나라도 안 되면 **미달성**으로 기록한다 |
| **없으면** | "될 것 같다"로 끝난다 |

**이 스킬이 개발 중 설계를 세 번 되돌렸다.** (5절 참조)

---

## 3. 실행 층 7개 — 기기의 일

### ⑤ `recipe_source` — 자료를 불러와 거른다

| | |
|---|---|
| **왜** | 조리 기록 3건은 우리가 만든 합성 데이터다. 그것만으로는 "데이터에서 판단했다"고 말할 수 없다 |
| **판단** | 식약처 공개 레시피 100건을 불러와 **기피 재료 · 나트륨 상한 · 조리법**으로 거른다.<br>**기피 재료는 파싱된 목록이 아니라 재료 원문 전체에서 찾는다** — `인삼(1뿌리)`처럼 수량 표기가 없어 구조화되지 않은 항목에도 알레르기 재료가 있을 수 있기 때문 |
| **실제** | 100건 → 알레르기 **25건** · 나트륨 300mg 초과 **27건** 제외 → **48건** |
| **없으면** | 합성 데이터 3건 안에서만 고른다. 근거가 우리가 만든 값뿐이다 |

### ⑥ `inventory` — 소진 임박 항목을 찾는다

| | |
|---|---|
| **왜** | **사용자가 시키기 전에 시작하려면 시작할 이유가 있어야 한다.** 그 이유를 재고에서 찾는다 |
| **판단** | `보관일수 ÷ 보관수명 ≥ 0.6` 이면 임박.<br>처음에는 보관일수만 봤는데 **된장 40일이 "급하다"고 나왔다.** 장류는 수명이 365일이라 소진율은 11%다. **비율로 바꿔서 고쳤다** |
| **없으면** | 사용자가 말을 걸어야 에이전트가 움직인다 = Zero-Touch가 아니다 |

### ⑦ `menu` — 재고와 기록을 대조해 후보를 만든다

| | |
|---|---|
| **왜** | 임박 재료를 알아도 그걸로 뭘 할지 정해야 한다 |
| **판단** | `점수 = 임박재료 수 × 10 − 부족분 수 × 3 + 만족도`<br>**기피 재료가 든 기록은 점수 계산 전에 제외한다** — 조달 단계에도 같은 필터가 있지만, 거기까지 가면 사용자가 확인을 받게 된다. 확인을 **안 받는 것**이 목표다<br>시간이 모자라기로 했으면 부족분 있는 후보도 제외 |
| **없으면** | 임박 재료가 안 쓰이고 버려진다 |

`satisfaction`이 개인 기록은 1~5, 공개 레시피는 0이다. **그래서 내 기록이 있으면 그게 먼저 뽑힌다.**

### ⑧ `procure` — 부족분을 조달한다 (여기가 안전 장치)

| | |
|---|---|
| **왜** | 자동 주문은 **되돌릴 수 없는 행동**이다. 에이전트가 마음대로 하면 안 되는 유일한 지점 |
| **판단** | 네 개 관문을 통과해야 자동 주문한다<br>1. 취급 품목인가<br>2. 기피·알레르기 목록에 없는가<br>3. **구매 이력이 있는가** (처음 사는 품목은 물어본다)<br>4. 1회 상한 **15,000원** 이내인가<br>하나라도 걸리면 **주문하지 않고 사용자에게 묻는다** |
| **실제** | 알레르기 가구에서 찹쌀·미나리가 3번에 걸려 **개입 2회**가 찍혔다 |
| **없으면** | 에이전트가 모르는 품목을 멋대로 산다 |

> **"개입 2회는 실패가 아니라 설계된 정지다."** 이 문장이 발표에서 중요하다.

### ⑨ `prep` — 계량하고 추가 수분을 예측한다

| | |
|---|---|
| **왜** | 같은 레시피라도 재료 상태가 다르면 **출발 질량이 다르다.** 그걸 모르고 조리하면 같은 목표를 다른 출발점에서 쫓는다 |
| **판단** | 수분 많은 채소(배추·애호박·무·양파·버섯)만 `실계량 × 0.02 × min(보관일수, 7)` 만큼 추가 수분을 예상한다.<br>처음엔 모든 재료에 적용했더니 **된장 40g에서 물 36g이 나온다는 결과**가 나왔다. 장류는 수분이 안 나온다 |
| **없으면** | 조리 단계가 엉뚱한 초기값으로 시작한다 |

### ⑩ `converge` — 관측값을 목표값으로 수렴시킨다 (재사용의 핵심)

| | |
|---|---|
| **왜** | **시간이 아니라 상태로 끝내야 한다.** 재료가 다르면 11분이 정답이 아니다 |
| **판단** | 매 분 관측 → 목표와 비교 → 도달하면 **즉시 종료**<br>아직 끓지 않으면(92℃ 미만) 화력을 올린다<br>끓는데도 남은 시간(ETA)이 6분을 넘으면 화력을 올린다 |
| **구조** | **스킬 안에 조리기도 건조기도 없다.** 관측 함수·액추에이터 함수·목표값을 인자로 받는다 |
| **실제** | 같은 코드로 조리기(질량비 0.78 → 8단계)와 건조기(함수율 0.08 → 11단계) |
| **없으면** | 고정 시간으로 돌린다 = 과조리하거나 덜 익는다 |

### ⑪ `aftercare` — 직전 오염도로 사후처리 코스를 고른다

| | |
|---|---|
| **왜** | **눌어붙음 정도는 조리기만 안다.** 그 정보를 넘기지 않으면 식기세척기는 모른 채 표준 코스를 돌리고, 덜 닦이면 다시 돌린다 |
| **판단** | 오염도 `< 0.30` 에코 / `< 0.55` 표준 / 그 이상 강력<br>**약한 코스를 돌렸을 때 다시 돌릴 확률**을 함께 계산한다 — 한 단계 부족 45%, 두 단계 부족 80% |
| **실제** | 오염도 0.62 → 강력(16.8L). 모르고 표준을 돌렸다면 11L + 재세척 45% × 16L = **기대 18.2L**. 즉 **1.4L 절감** |
| **없으면** | 물을 아끼려다 다시 돌려서 더 쓴다 |

> 처음엔 "물 절감 −5.0L"라는 **음수**가 나왔다. 강력 코스가 물을 더 쓰니까.
> **재세척 확률을 포함한 기대값**으로 바꾸고 나서야 숫자가 말이 됐다.

---

## 4. 플래너 — 순서를 적지 않고 계산하는 법

각 스킬은 **자기가 뭘 필요로 하고 뭘 만드는지만** 선언한다. 다른 스킬은 모른다.

```
inventory      없음        → 임박 목록
recipe_source  없음        → 후보군
menu           임박·후보군  → 기록 · 부족분
procure        부족분      → 재고 확보
prep           재고확보·기록 → 계량값
converge       계량값      → 조리 · 오염도
aftercare      오염도      → 세척
```

플래너는 **목표에서 거꾸로** 탄다.

```
목표 = 세척
  세척을 만드는 건? aftercare → 오염도가 필요
  오염도를 만드는 건? converge → 계량값이 필요
  ... → inventory 까지 전부 끌려온다   = 7단계
```

**1인 가구는 `재고 확보`가 "이미 참"이다.** (조달을 빼기로 했으니)
그러면 procure를 끌어올 이유가 사라진다 → **6단계**.

> 순서를 적어둔 곳이 코드에 없다. 이게 "스스로 계획한다"의 실제 의미다.

---

## 5. 무엇을 측정했나

### 측정 1 — 흔들림 (단일 실행을 근거로 쓰지 않기)

시뮬레이터 난수는 계량 ±3%, 증발 ±8%, 건조 ±10% 세 곳에 있다.
**잡음 크기를 모르면 어떤 수치도 해석할 수 없어서** 시드를 바꿔 반복했다.

| 확률적인 값 | 결과 |
|---|---|
| 조리기 8단계 | 500회 중 **499회** |
| 건조기 11단계 | 500회 중 **486회** |
| 최종 질량비 | 0.7295 ~ 0.7796 (목표 0.78) |

| 결정적인 값 (200회 × 3상황 = 600회) | 결과 |
|---|---|
| 계획 순서 · 선택 메뉴 · 세척 코스 · 개입 횟수 | **전부 불변** |

> **처음엔 "시드 3회"를 계획했다.** n=3이면 표준오차 자체의 오차가 50%라 분산 추정에 못 쓴다.
> 시뮬레이터는 1회 390ms라 3회로 제한할 이유가 없어서 500회/600회로 올렸다.

### 측정 2 — 같은 스킬, 다른 기기

`converge` 하나를 조리기와 건조기에 **같은 코드로** 돌렸다. 바꾼 건 주입한 관측·액추에이터 함수뿐.
`aftercare`도 코스 프로파일만 바꿔 식기세척기·세탁기에 쓰인다.

### 측정 3 — 공개 자료 필터링

식약처 레시피 100건 → 알레르기 25건 · 나트륨 27건 제외 → 48건.
제외된 메뉴가 이름으로 찍힌다 (새우 두부 계란찜, 사과 새우 북엇국 …).

### 측정 4 — 상황 3건에 대한 계획 차이

| 상황 | 계획 | 개입 | 장면 |
|---|---|---|---|
| 야근 1인 | 6단계 (조달 제외) | 0회 | 4/4 |
| 맞벌이 2인 | 7단계 | 0회 | 4/4 |
| 알레르기 4인 | 7단계 | 2회 | 5/5 |

---

## 6. 검증 스킬이 실제로 잡은 것

`experience_verify`는 장식이 아니다. 만드는 동안 **설계를 세 번 되돌렸다.**

| 무엇 | 어떻게 드러났나 | 조치 |
|---|---|---|
| 시나리오엔 "미리 제외한다"고 썼는데 실행은 조달까지 가서 확인 요구 | **5장면 중 3장면** | `menu` 단계로 필터를 앞당김 |
| 공개 레시피가 뽑힌 경로에서 실행 중단 | **종료코드 1** | 공개 레시피용 변환 경로 추가 |
| `prep`이 소금·후춧가루를 재고없음 처리 → 조리 실패 | **5장면 중 3장면** | 상비품을 재고 초기 상태에 포함 |

세 번째는 상비품 처리가 `menu`에만 있고 `prep`에는 없어서 생겼다.
**같은 가정을 두 곳에서 다르게 구현하면 생기는 결함**이고, 실행 대조가 없었으면 못 찾았다.

---

## 7. 안 한 것 (먼저 말해야 할 한계)

- **기기는 시뮬레이터다.** ThinQ Connect API 규격으로 교체 가능한 형태지만 실물이 아니다
- **개인 조리 기록 3건은 합성 데이터다.** 공개 레시피 100건만 실데이터
- **관측 주기가 1분이라 목표를 지나친다.** 최종 질량비가 0.78 목표에 0.7295까지 내려간다
- **알레르기 검사는 키워드 매칭이다.** "칵테일새우"는 잡지만 "쉬림프"는 놓친다
- **LLM이 스킬을 고르는 경로(`agent.py`)는 검증하지 않았다.** API 키가 없어 한 번도 실행하지 못했고, 그래서 제출 경로로 쓰지 않았다
- **흔들림은 시뮬레이터 난수에 대한 것**이지 실제 조리의 변동이 아니다

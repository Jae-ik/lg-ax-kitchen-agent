# Agent 정의서

**팀 독립선언문 · Theme B (UX/UI 시나리오 설계자)**
작성 2026-09-22 · 모든 수치는 `run_design.py` 실행 결과(`scenarios.json`)에서 인용

---

## 1. 이 Agent는 누구의 어떤 수고를 덜어주는가

| 항목 | 내용 |
|---|---|
| **역할** | UX/UI 시나리오 설계자 — 고객 상황을 읽고 가전의 할 일을 나눠 하나의 경험으로 설계한다 |
| **사용자** | LG전자 키친솔루션·빌트인쿠킹솔루션 사업부의 UX 시나리오 설계 담당자 |
| **덜어주는 수고** | 가구 형태·생활 리듬·제약이 바뀔 때마다 시나리오를 처음부터 다시 그리고, 그것이 실제 가전 동작으로 성립하는지 일일이 확인하는 일 |
| **왜 지금 필요한가** | 제품은 늘어나는데 고객의 하나의 경험으로 묶는 일은 더 어려워진다. 상황의 가짓수만큼 시나리오가 필요하지만 사람이 그 수를 감당할 수 없다 |

> 이 Agent가 만드는 결과물이 곧 미래 키친 콘셉트다.
> 콘셉트를 사람이 쓰고 Agent가 거드는 것이 아니라, **Agent가 반복 생성한다.**

---

## 2. Input / Process / Output

### Input — 지시가 아니라 **상황**

```json
{ "label": "야근이 잦은 1인 가구", "arrive_home": "21:40",
  "time_budget_min": 25, "next_morning_rush": true,
  "avoid": [], "dislike_noise_after": "22:30",
  "friction_reported": ["냉장고를 열어 뭐가 남았는지 확인하는 일", ...],
  "fridge": [ {"name": "배추", "stored_days": 5, "shelf_life_days": 7}, ... ] }
```

"된장찌개 해줘" 같은 지시를 받지 않는다. 상황이 들어오면 목표는 Agent가 세운다.

### Process — Goal → Plan → Tool·Skill → Execute → Evaluate → Output

| 단계 | 하는 일 | 근거를 남기는 방식 |
|---|---|---|
| **Goal** | 상황에서 "이 고객의 수고를 줄이는 시나리오를 만들고 검증한다"를 목표로 세운다 | 사용자 지시 `null`, 트리거는 상황 입력 |
| **Plan** | 설계 4단계의 순서를 **전제조건에서 계산**한다 | `planner.plan()` 이 목표 사실에서 역방향 탐색 후 위상 정렬 |
| **Tool·Skill** | 레지스트리에서 필요한 스킬을 조회해 고른다 | `REGISTRY` — 스킬을 추가해도 Agent 코드는 바뀌지 않는다 |
| **Execute** | 설계 스킬이 실행 스킬을 주입받아 실제로 돌린다 | 시뮬레이터가 단계별 상태를 반환 |
| **Evaluate** | 시나리오의 각 장면을 실행 로그와 대조한다 | 장면마다 `verified_by` 스킬의 성공 여부를 확인 |
| **Output** | UX 시나리오 + 실행 지표 | `scenarios.json`, `trace_design.json` |

### Output — UX 시나리오와 그것을 뒷받침하는 실행 결과

```
21:40  사용자: 현관에 들어선다
       가전: 재고와 남은 시간을 이미 읽고 오늘 할 수 있는 것을 정해 둔다
       사라진 수고: 냉장고를 열어 뭐가 남았는지 확인하는 일   [inventory 실행 성공]
```

---

## 3. Skill 구성 — 설계 층 4개 + 실행 층 6개

| 층 | Skill | 필요(requires) | 생성(provides) |
|---|---|---|---|
| 설계 | `situation_read` | — | friction, constraints |
| 설계 | `scenario_draft` | friction, constraints | scenario |
| 설계 | `flow_design` | scenario, constraints | flow |
| 설계 | `experience_verify` | flow, scenario | verified |
| 실행 | `recipe_source` | — | recipe_pool |
| 실행 | `inventory` | — | urgent_items |
| 실행 | `menu` | urgent_items, recipe_pool | chosen_record, missing_items |
| 실행 | `procure` | missing_items | stock_complete |
| 실행 | `prep` | stock_complete, chosen_record | measured |
| 실행 | `converge` | measured | cooked, soil_score |
| 실행 | `aftercare` | soil_score | cleaned |

설계 층은 실행 층을 `import` 하지 않는다. 실행 함수를 **주입**받는다.
그래서 같은 설계 층을 세탁실·욕실 도메인에 그대로 얹을 수 있다.

---

## 4. 반복 수행의 증거 — 상황 3건, 계획 3종

| 고객 상황 | 계산된 가전 작업 순서 | 선택 메뉴 | 개입 | 장면 |
|---|---|---|---|---|
| 야근 1인 가구 | inventory → recipe_source → menu → prep → converge → aftercare | 된장찌개 (저장 기록) | 0회 | 4/4 |
| 맞벌이 2인 가구 | inventory → recipe_source → menu → **procure** → prep → converge → aftercare | 된장찌개 (저장 기록) | 0회 | 4/4 |
| 알레르기 4인 가구 | inventory → recipe_source → menu → **procure** → prep → converge → aftercare | 호박잎 삼계탕 (**공개 레시피**) | 2회 | 5/5 |

- 1인 가구는 **귀가 후 25분**뿐이라 조달(예상 20분)이 예산을 넘는다. Agent가 `skip_procurement`로 판단하고 `stock_complete`를 이미 성립한 사실로 두자 **플래너가 `procure`를 계획에서 스스로 제외**했다. 순서를 적어둔 곳은 코드 어디에도 없다.
- 알레르기 가구는 저장 기록에 쓸 것이 없어 **공개 레시피에서 골랐다.** 임박한 닭고기(잔여 1일)를 쓰는 호박잎 삼계탕이다. 개인 기록이 없으므로 목표 질량비는 조리법 기본값(가정)을 썼고, 이번 조리의 실측이 다음부터 그 자리를 대신한다.
- 같은 가구에서 **자동 주문하지 않고 물은 것이 2건**이다 — 찹쌀은 제때 오는 상점이 없고(최속 720분 > 남은 60분), 미나리는 처음 사는 품목이다. 사유가 서로 다르며, 개입 2회는 실패가 아니라 설계된 정지다.

### 실행 지표 (시뮬레이터)

| 지표 | 1인·2인 가구 | 알레르기 4인 가구 |
|---|---|---|
| 가열 시간 | 8분 (기록 고정 11분 대비 3분 단축) | 5분 (첫 조리 — 비교할 실측 기록 없음) |
| 최종 질량비(먹기 직전) | 0.78 (목표 0.78) | 0.85 (목표 0.85, 조리법 기본값) |
| 세척 기대 물 | 8.3 L (에코, 기본 코스 대비 3.5 L 절감) | 16.8 L (강력, 1.4 L 절감) |
| 자동 주문 | 0원 / 2,800원 | 0원 (2건 모두 확인 요청) |

> 이 수치는 **시뮬레이터 상의 결과이며 실측이 아니다.** 물리 모델은 개념 확인용이다.

---

## 4-1. 데이터 근거 — 합성이 아닌 공개 자료

`recipe_source` 스킬이 **식품의약품안전처 조리식품의 레시피 DB(COOKRCP01)** 를 불러와 거른다.

| 항목 | 값 |
|---|---|
| 적재 | 100건 (공개 `sample` 키 · 전체 1,156건 중) |
| 알레르기·기피(표고버섯·새우)로 제외 | **25건** |
| 나트륨 300mg 초과로 제외 | **27건** |
| 최종 후보 | **48건** |

기피 재료는 파싱된 재료 목록이 아니라 **재료 원문 전체**에서 찾는다. 수량 표기가 없어 구조화되지 않은 항목(`인삼(1뿌리)`, `대추(3알)`)에도 알레르기 재료가 있을 수 있는데, 안전 판정에서 그것을 놓치면 안 되기 때문이다.

수집은 `fetch_data.py`가 하고 결과는 `data/recipes.json`에 캐시된다. **평소 실행에는 네트워크가 필요 없다.**
발급키(`MFDS_KEY`, 무료)를 넣으면 전체 1,156건을 받는다.

> `sample` 키는 같은 요청에 5건 또는 100건을 번갈아 준다(서버 동작). 한 응답을 믿지 않고 여러 번 받아 `RCP_SEQ` 기준으로 병합한다.

---

## 4-2. 흔들림 측정 — 단일 실행을 근거로 쓰지 않기 위해

시뮬레이터에는 난수가 세 곳 들어간다. 계량 오차 ±3%, 증발 편차 ±8%, 건조 편차 ±10%.
잡음 크기를 모르면 어떤 수치도 해석할 수 없으므로, **시드를 바꿔 반복**해 분포를 먼저 쟀다.
재현: `python measure_variance.py 500 200` → `variance.json`

### 확률적인 것 — converge 스킬, 시드 200회

| 지표 | 중앙 | 범위 | 분포 |
|---|---|---|---|
| 조리기 가열 시간 | **7.6분** | 7.35~7.9 | 표준편차 0.105 |
| 조리기 최종 질량비 | 0.7814 | **0.7775~0.7851** | 표준편차 0.0019 |
| 건조기 건조 시간 | **11.4분** | 11.0~11.75 | 표준편차 0.153 |
| 건조기 최종 함수율 | 0.0776 | 0.0716~0.0800 | 표준편차 0.0014 |

본문에 인용한 "7.65분 / 11.3분"은 단일 실행값이며, 200회 중앙값은 7.6분·11.4분이다.

### 결정적인 것 — 설계 에이전트, 40회 × 4상황 = 160회

| 항목 | 결과 |
|---|---|
| 계획 순서 | **160회 전부 불변** |
| 선택 메뉴 · 세척 코스 | 불변 |
| 사용자 개입 | 불변 (0 / 0 / 2회) |
| 장면 달성률 | 불변 (전부 100%) |
| 가열 시간 | 8분 199/200 · 8분 199/200 · 5분 200/200 |

계량과 증발에 난수가 들어가는데도 **계획이 한 번도 바뀌지 않았다.**
계획이 난수가 아니라 전제조건에서 나온다는 뜻이며, 단일 실행의 우연이 아니다.

### 이 측정이 드러낸 결함 두 가지

1. **측정 코드 자체의 설정 불일치.** 처음에는 converge 단독 측정이 7단계, 파이프라인이 8단계로
   어긋났다. 단독 측정에서 조달 단계를 건너뛰어 두부가 계량에서 빠져 있었다. 고치지 않았으면
   "조리기 7단계"라는 틀린 수치를 발표에 넣을 뻔했다.
2. **목표를 지나친 뒤 멈췄다.** 최종 질량비가 목표 0.78에 대해 **0.7295까지** 내려갔다. → 관측 주기를 줄이고, 기기가 여열을 예측해 미리 끄게 하고, 지나치면 물로 되돌리도록 고쳐 목표 양옆 0.7775~0.7851 로 좁아졌다.
   1분 간격으로만 관측하므로 그 사이에 목표를 통과하면 다음 관측에서야 멈춘다.
   "목표에 도달하면 즉시 종료한다"는 서술은 관측 주기 안에서만 참이다.

## 5. 한계 (현재 확인된 것)

- **개인 조리 기록 3건은 합성 데이터다.** 공개 레시피 100건은 실제 공공 자료지만, "사용자가 만족한 세션의 목표 질량비"는 실측이 필요하다.
- 공개 레시피에는 목표 질량비가 없어 **조리법별 기본값(가정)** 을 쓴다. 첫 조리의 실측으로 대체되는 구조지만, 그 기본값 자체는 검증되지 않았다.
- 재료명 표기가 자료마다 달라(두부/연두부, 닭고기/닭가슴살) **별칭 사전**으로 맞춘다. 사전에 없는 표기는 놓친다.
- 상비품(소금·후춧가루 등)은 늘 있다고 가정한다. 실제로는 사용자 등록이나 소모량 추정이 필요하다.
- **여전히 오차가 남는다.** 최종 질량비가 0.7775~0.7851 로 흔들린다. 여열 예측과 물 보정으로 0.7295~0.7796(한쪽으로 치우침) 에서 목표 양옆 ±0.005 로 좁혔으나 0 은 아니다.
- 기기 동작은 시뮬레이터다. ThinQ Connect API 규격에 맞춰 교체 가능한 형태지만, **조리기기의 가열 제어가 개인용 API에서 어디까지 열려 있는지는 확인하지 못했다.**
- 흔들림은 **시뮬레이터 난수에 대한 것**이지 실제 조리의 변동이 아니다. 실물에서는 재료·화구·용기 차이가 더 큰 변동을 만들 수 있다.
- LLM 기반 계획 경로(`agent.py`)는 API 키가 없어 실행 검증되지 않았다. 현재 제출 경로는 **LLM 없이도 동작하는 결정적 플래너**다.

## 6. 개발 중 검증 스킬이 잡아낸 것

`experience_verify`는 장식이 아니다. 만드는 동안 실제로 세 번 설계를 되돌렸다.

| 잡아낸 것 | 판정 | 조치 |
|---|---|---|
| 시나리오에 "못 먹는 재료를 미리 제외한다"고 써 놓고 실행은 조달까지 가서 확인을 요구 | 5장면 중 3장면 | `menu` 단계로 필터를 앞당김 |
| 공개 레시피가 선택된 경로에서 `record_get`이 그 ID를 몰라 실행이 중단 | 종료코드 1 | 공개 레시피용 변환 경로 추가 |
| `prep`이 소금·후춧가루를 재고 없음으로 보고 조리 실패 | 5장면 중 3장면 | 상비품을 재고 초기 상태에 포함 |

세 번째는 `menu`에만 상비품 처리가 있고 `prep`에는 없어서 생긴 불일치였다. **같은 가정을 두 곳에서 다르게 구현하면 생기는 종류의 결함**이고, 실행 대조가 없었으면 "왜인지 모르게 안 되는" 상태로 남았을 것이다.

# 실행 영상 대본 (3~5분)

**요구**: "입력 → 판단 → Tool 사용 → 결과까지 한 번에"
**준비**: 터미널 하나, 글꼴 크게(14pt 이상), 작업 폴더 `agent/`
**원칙**: 말로 설명하지 말고 **화면에 뜬 것을 가리킨다.** 화면에 없는 건 말하지 않는다.

녹화 전 한 번 확인
```bash
cd agent
python run_design.py > /dev/null && echo OK
```

---

## 0:00 – 0:35 · 무엇을 만들었는지 한 문장

> "저희 에이전트는 **UX 시나리오를 설계하는 직무**를 수행합니다.
> 입력은 '된장찌개 해줘' 같은 지시가 아니라 **고객 상황**이고,
> 출력은 **UX 시나리오와 그것을 실제로 돌린 결과**입니다."

화면: `personas.py`를 열어 `p1_야근` 블록을 보여준다.

> "귀가 21시 40분, 쓸 수 있는 시간 25분, 다음 날 아침에 여유 없음.
> 이게 입력의 전부입니다. 무엇을 하라는 지시는 없습니다."

---

## 0:35 – 1:40 · 계획이 **계산**되는 장면 (핵심)

```bash
python run_design.py p1_야근
```

`[PLAN]` 블록에서 멈춘다.

> "설계 4단계의 순서도 코드에 적혀 있지 않습니다.
> 각 스킬이 무엇을 필요로 하고 무엇을 만드는지만 선언하면
> 플래너가 목표에서 역방향으로 타고 올라가 순서를 만듭니다."

`flow_design`의 `[EVALUATE]` 근거를 가리킨다.

> "여기가 이번 제안의 핵심입니다. **조달 단계가 빠졌습니다.**
> 귀가 후 25분인데 조달에 20분이 걸리니 예산을 넘습니다.
> 에이전트가 '재고 안에서 해결한다'고 판단하자,
> **플래너가 조달을 계획에서 스스로 제외**해 여섯 단계가 됐습니다.
> 제가 뺀 게 아니라 전제조건에서 계산된 결과입니다."

---

## 1:40 – 2:40 · 자료 수집과 안전 정지

```bash
python run_design.py p3_알레르기
```

`recipe_source`의 `[EVALUATE]`를 가리킨다.

> "식품의약품안전처 조리식품 레시피 DB를 불러옵니다. 실제 공공 데이터 100건입니다.
> 기피 재료가 든 **25건**, 나트륨 상한을 넘는 **27건**을 걸러 **48건**을 남겼습니다.
> 제외된 메뉴 이름이 그대로 찍힙니다 — 새우 두부 계란찜, 사과 새우 북엇국."

`procure` 출력을 가리킨다.

> "찹쌀과 미나리는 **처음 사는 품목**이라 자동으로 주문하지 않고 사용자에게 물었습니다.
> 개입 2회가 찍혀 있는데, 이건 실패가 아니라
> **되돌릴 수 없는 행동 앞에서 멈추도록 설계한 결과**입니다."

마지막 비교표를 보여준다.

> "같은 코드에 상황만 바꿔 넣었더니 계획이 셋 다 다릅니다."

---

## 2:40 – 3:30 · 같은 스킬, 다른 가전

```bash
python run_demo.py
```

맨 아래 `재사용 증거` 블록.

> "converge 스킬 하나를 조리기와 건조기에 **같은 코드로** 돌렸습니다.
> 조리기는 목표 질량비 0.78에 7.8분, 건조기는 함수율 0.08에 10.8분.
> 바꾼 건 관측 함수와 액추에이터 함수뿐입니다."

`skills/core.py`를 열어 `ConvergeSkill.run` 상단을 보여준다.

> "스킬 안에 조리기도 건조기도 없습니다. 무엇을 관측하고 무엇을 조작할지는 주입받습니다."

---

## 3:30 – 4:30 · 단일 실행을 근거로 쓰지 않았다

```bash
python measure_variance.py 500 200
```

(2~3분 걸리므로 **미리 돌려 두고** `variance.json` 또는 저장된 출력을 보여준다)

> "수치를 제안서에 적기 전에 흔들림을 먼저 쟀습니다.
> 시드를 바꿔 조리기·건조기를 500회, 설계 에이전트를 상황마다 200회씩 800회 돌렸습니다."

| 보여줄 값 | 말할 내용 |
|---|---|
| 가열 7.8분(7.6~8.1) · 건조 10.8분(10.3~11.2) | "제안서에 쓴 수치가 어디서 나오는지" |
| 계획·메뉴·개입 **800회 전부 불변** | "계획이 난수가 아니라는 증거" |
| 최종 질량비 0.7669~0.7800 | **"목표를 지나치는 폭이 최대 0.013입니다"** |

> "마지막 줄은 저희 약점입니다. 1분 간격으로만 관측하니
> 목표를 통과한 뒤 다음 관측에서 멈춥니다.
> '목표에 도달하면 즉시 종료'는 관측 주기 안에서만 참입니다.
> 이건 이번 측정에서 처음 알았고, 제안서 6절에 한계로 적었습니다."

---

## 4:30 – 5:00 · 마무리

```bash
python -c "import sys; print(sys.version)"
pip list 2>NUL | findstr /i anthropic
```

> "제출 경로는 **표준 라이브러리만** 씁니다. 설치할 것도, 유료 API도, 키도 없습니다.
> 네트워크를 끊어도 끝까지 돕니다 — 공개 자료는 캐시로 동봉했습니다.
> 심사하시는 분 노트북에서 `python run_design.py` 한 줄로 그대로 재현됩니다."

---

## 하지 말 것

- **"AI가 알아서 한다"** 같은 말 — 화면에 근거가 없다
- 시뮬레이터를 실물처럼 말하기 — **"시뮬레이터입니다"** 를 한 번은 분명히 말한다
- 5분 초과 — 넘칠 것 같으면 3:30–4:30 흔들림 측정을 30초로 줄인다

## 질문받을 것에 대한 답

| 질문 | 답 |
|---|---|
| "LLM이 어디 있나요?" | "계획은 LLM이 아니라 전제조건 플래너가 계산합니다. 그래서 800회를 돌려도 같은 계획이 나옵니다. LLM으로 스킬을 고르는 경로(`agent.py`)도 있지만 API 키가 없어 검증하지 못했고, 제출 경로가 아닙니다." |
| "실제 가전에서 되나요?" | "아닙니다, 시뮬레이터입니다. ThinQ Connect API 규격에 맞춰 교체 가능한 형태로 뒀고, `kitchen_domain.py`의 바인딩 함수 6개만 바꾸면 됩니다. 조리기기 가열 제어가 개인용 API에서 어디까지 열려 있는지는 확인하지 못했습니다." |
| "데이터가 진짜인가요?" | "공개 레시피 100건은 식약처 실데이터입니다. 개인 조리 기록 3건은 합성이고, 그건 한계로 적었습니다." |
